apiVersion: v1
kind: Service
metadata:
  name: {{ include "gulfcam-backend-chart.fullname" . }}
  labels:
    {{- include "gulfcam-backend-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "gulfcam-backend-chart.selectorLabels" . | nindent 4 }}
